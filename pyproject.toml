[project]
name = "solectrac"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "canalystii>=0.1",
    "pyserial>=3.0",
    "python-can>=4.0",
    "rich>=13.0",
]
